#include "headers/calc.hpp"

using namespace home;

int calculate::add(int a1, int a2) { 
    return(a1 + a2);
}

int calculate::multiply(int a1, int a2) { 
    return(a1 * a2);
}

int calculate::kill(int a1, int a2) { 
    return(0);
}